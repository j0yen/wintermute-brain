/target
**/*.rs.bk
Cargo.lock.bk
.DS_Store
# subagent isolation worktrees (Claude Code Agent --isolation=worktree
# creates these inside the slice; never part of the slice artifact)
.claude/worktrees/

# Runtime artifacts — recall sqlite created at cwd when wmd / tests open
# a RecallClient against a relative socket / spawn embedded recall.
/index/
